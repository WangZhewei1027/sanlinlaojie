# 近10天更新汇总（2026-05-11 ~ 2026-05-20）

## sanlinlaojie（Web 端）

### ✨ Feature

| 日期 | 内容 |
|------|------|
| 05-12 | 微信小程序二维码功能；PATCH 请求新增 `file_url` 支持；AssetEditor 依赖更新 |
| 05-15 | 组织数据接口新增 `text_asset_miniapp_style`；AssetEditor 新增 `scale_multiplier` 支持 |
| 05-17 | 新增权限模型文档与 Workspace 配置说明 |
| 05-18 | 3D 模型预览新增加载态与错误处理；实现"All workspaces"全局工作区功能 |
| 05-19 | 新增视频上传与预览组件；小程序配置页新增纸屑特效开关；新增音频格式兼容性文档；用户注册脚本支持组织/工作区分配；MDX 文档增强（用户引导、平台概览）；音频文件暂时禁用压缩 |
| 05-20 | 集成 `@tailwindcss/typography` 并更新 MDX 组件；QR 码生成环境更新为 `release`；VSCode 新增 `.mdx` 文件关联 |

### 🐛 Fix

| 日期 | 内容 |
|------|------|
| 05-17 | 修复 Supabase 环境变量缺失时的错误提示信息 |

### 🔧 Refactor / Chore

| 日期 | 内容 |
|------|------|
| 05-12 | 代码重构 |
| 05-19 | 添加 MDX 依赖 |

---

## xr-frame-plant-trees（小程序端）

### ✨ Feature

| 日期 | 内容 |
|------|------|
| 05-15 | `setConfig` 更新 `organizationId` 时清除 `workspaceId` |
| 05-17 | 模型与音频资源规范化新增 `scale_multiplier` 支持 |
| 05-18 | `loadPersistedScanConfig` 明确包含 `workspaceId`；获取大资源时新增 `organizationId` 必传、`workspaceId` 可选 |
| 05-19 | **大更新（性能优化）**：实现串行资源放置队列减少掉帧；新增节点间距斥力模块防止重叠；素材放置确保出现在相机前方；新增视频资产与透明视频特效支持；组织配置驱动的随机纸屑特效；音频音量调整逻辑优化；新增性能优化文档与资源复用策略图 |

### 🔧 Refactor

| 日期 | 内容 |
|------|------|
| 05-11 | 代码重构 |

---

## 总结

这10天两端同步推进：

- **Web 端**重心在文档完善（MDX 支持、权限模型）、视频上传、多工作区管理以及 QR 码环境配置。
- **小程序端**重心在 AR 性能优化（串行放置队列、斥力算法防重叠）以及视频/透明视频特效能力落地。
